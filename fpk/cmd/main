#!/bin/bash
set -u

CONTAINER="fnos-photo-auto-rotate"

case "${1:-}" in
  start|stop)
    exit 0
    ;;
  status)
    docker inspect "$CONTAINER" 2>/dev/null | grep -q '"Status": "running"' && exit 0
    exit 3
    ;;
  *)
    exit 1
    ;;
esac
